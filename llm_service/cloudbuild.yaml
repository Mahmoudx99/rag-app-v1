steps:
  # Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'build',
      '-t',
      'me-central2-docker.pkg.dev/anb-gpt-prj/cloud-run-source-deploy/rag-llm',
      '.'
    ]

  # Push to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'push',
      'me-central2-docker.pkg.dev/anb-gpt-prj/cloud-run-source-deploy/rag-llm'
    ]

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args: [
      'run',
      'deploy',
      'rag-llm',
      '--image=me-central2-docker.pkg.dev/anb-gpt-prj/cloud-run-source-deploy/rag-llm:latest',
      '--region=me-central2',
      '--platform=managed',
      '--memory=2Gi',
      '--cpu=2',
      '--min-instances=0',
      '--max-instances=5',
      '--port=8001',
      '--allow-unauthenticated',
      '--set-env-vars=LLM_PROVIDER=vertex_ai',
      '--set-env-vars=LLM_MODEL=llama-3.1-8b',
      '--set-env-vars=LLM_MAX_TOKENS=4096',
      '--set-env-vars=LLM_TEMPERATURE=0.7',
      '--set-env-vars=VERTEX_AI_PROJECT_ID=anb-gpt-prj',
      '--set-env-vars=VERTEX_AI_LOCATION=me-central2',
      '--set-env-vars=VERTEX_AI_LLM_ENDPOINT_ID=YOUR_ENDPOINT_ID'
    ]

images:
  - 'me-central2-docker.pkg.dev/anb-gpt-prj/cloud-run-source-deploy/rag-llm'
